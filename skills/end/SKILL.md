---
name: end
description: Shorthand alias for write-session-log — memorialize the Session at a work boundary. User-invoked ("/end") only; write-session-log's own triggers still apply.
disable-model-invocation: true
---

# end

Alias — shorter to type, same behavior. Invoke the **write-session-log** skill (installed alongside this one) and follow it exactly. If skills can't be invoked by name in this harness, read `../write-session-log/SKILL.md` relative to this file (typically `~/.agents/skills/write-session-log/SKILL.md`) and follow that.
