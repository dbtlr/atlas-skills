---
name: start
description: Shorthand alias for start-session — assemble the Session Primer and route the work. User-invoked ("/start") only; start-session itself auto-triggers at session start.
disable-model-invocation: true
---

# start

Alias — shorter to type, same behavior. Invoke the **start-session** skill (installed alongside this one) and follow it exactly. If skills can't be invoked by name in this harness, read `../start-session/SKILL.md` relative to this file (typically `~/.agents/skills/start-session/SKILL.md`) and follow that.
